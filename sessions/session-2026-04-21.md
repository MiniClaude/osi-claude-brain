# Session 2026-04-21 — OneDrive to GitHub Migration

## What we did
Finished the OneDrive-to-GitHub migration. Files were already pushed to `github.com/Drrewdy/Claude-Brain` in a prior session, but all the docs still described the old OneDrive workflow. Rewrote 12 files to match reality:
- `CLAUDE.md` — new Git workflow section, path updated to `C:\Claude-Brain\`, OneDrive demoted to archive
- `CLAUDE.txt` — collapsed to a pointer (it was a stale duplicate of CLAUDE.md)
- `skills/README.md` — rewritten for Git workflow
- 9 skill `SKILL.md` files — SYNC NOTE updated, `email-queue.json` path changed to `C:\Claude-Brain\email-queue.json`, OneDrive-safe Python fallbacks removed (no longer needed on local disk)

Commit `2f73b2c` on main. 12 files, +84 / -304.

## Action items
- Install Git on second laptop, then `git clone https://github.com/Drrewdy/Claude-Brain.git C:/Claude-Brain` there. Point Cowork at the new path on that machine too.
- Old OneDrive copies: leave as read-only archive. Do not write.
- Daily habit: `git pull` before starting, `git add . && git commit -m "..." && git push` when done.

## HubSpot
Nothing to update.
